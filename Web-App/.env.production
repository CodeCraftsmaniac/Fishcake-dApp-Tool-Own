# Production Environment Variables
# Backend API URL (Oracle Cloud VM: neil-blumenthal)
# Note: Browser requests use /api/proxy route to avoid mixed-content issues
# Server-side rendering uses direct HTTP to VM (Vercel → Oracle)
NEXT_PUBLIC_API_URL=http://129.80.144.145:3001
